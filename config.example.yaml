# SMT - Schema Migration Toolkit Configuration
# Copy this file and update with your actual values:
#   cp config.example.yaml smt.yaml

# =============================================================================
# Source Database
# =============================================================================
source:
  dialect: postgresql       # postgresql | mssql
  # driver: psycopg2        # optional: defaults per dialect (psycopg2, pyodbc)
  host: localhost
  port: 5432
  user: postgres
  password: YourPassword    # or set SMT_SOURCE_PASSWORD env var
  database: StackOverflow2010
  schema: dbo

# =============================================================================
# Target Database
# =============================================================================
# Target schema is auto-derived as: <source_host>__<source_database>__<source_schema>
# (all lowercased; characters outside [a-z0-9_] in the host are replaced with _)
# Example: localhost__stackoverflow2010__dbo
# To override, set the top-level `target_schema:` field below.
target:
  dialect: postgresql
  # driver: psycopg2
  host: localhost
  port: 5432
  user: postgres
  password: YourPassword    # or set SMT_TARGET_PASSWORD env var
  database: stackoverflow

# =============================================================================
# Target Schema (optional)
# =============================================================================
# Uncomment to override the auto-derived target schema name.
# target_schema: my_custom_schema

# =============================================================================
# Tables to Migrate
# =============================================================================
# List of table names, or "all" to migrate every table in the source schema
tables:
  - Users
  - Posts
  - Comments

# =============================================================================
# Working Directory
# =============================================================================
# Directory where migration artifacts are created
# (models/ package, alembic config, migration files, per-table DDL exports)
workspace: ./migration_workspace
