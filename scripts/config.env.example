
# Schema Migration Configuration
# Copy this file to config.env and update with your actual values:
#   cp config.env.example config.env

# =============================================================================
# Source Database (PostgreSQL)
# =============================================================================
SOURCE_PG_HOST=localhost
SOURCE_PG_PORT=5432
SOURCE_PG_USER=postgres
SOURCE_PG_PASSWORD=YourPassword
SOURCE_PG_DATABASE=StackOverflow2010
SOURCE_PG_SCHEMA=dbo

# =============================================================================
# Target Database (PostgreSQL)
# =============================================================================
# Target schema is auto-derived as: dw__<database>__<schema> (lowercase)
# Example: dw__stackoverflow2010__dbo
TARGET_PG_HOST=localhost
TARGET_PG_PORT=5432
TARGET_PG_USER=postgres
TARGET_PG_PASSWORD=YourPassword
TARGET_PG_DATABASE=stackoverflow

# =============================================================================
# Tables to Migrate
# =============================================================================
# Comma-separated list of tables, or "all" to migrate all tables
# Available tables in StackOverflow2010:
#   Users, Posts, Comments, Badges, Votes, VoteTypes, PostLinks, PostTypes, LinkTypes
TABLES=Users,Posts,Comments

# =============================================================================
# Working Directory
# =============================================================================
# Directory where migration artifacts will be created
# (venv, models.py, alembic config, etc.)
WORK_DIR=./migration_workspace
